# Sagarika Workspace

This repository now keeps the HYROX prep app isolated from older workspace assets.

## HYROX App

```text
hyrox-40-dashboard/
```

Run it from that folder:

```bash
cd hyrox-40-dashboard
npm install
npm run dev
```

The old image assets, if still needed, remain outside the HYROX app under root `public/images`.
